# Dependencies (installed by the build; not committed)
node_modules/

# OS / editor noise
.DS_Store

# Local env — the Sanity project id lives in studio/.env (gitignored there too).
# In CI it's the public SANITY_PROJECT_ID in .github/workflows/rebuild.yml (no secret).
.env
.env.local

# Sanity Studio build artifacts
studio/dist/
studio/.sanity/
