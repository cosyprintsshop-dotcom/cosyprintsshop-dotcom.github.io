{
  "name": "cosyprints-site",
  "private": true,
  "type": "module",
  "description": "cosyprints.com — static site + compliant catalogue sync (no scraping; manual source files, see README)",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "import:sanity": "node scripts/fetch-sanity.js",
    "seed:sanity": "node scripts/seed-sanity.js",
    "import:leboncoin": "node scripts/import-leboncoin.js",
    "import:vinted": "node scripts/import-vinted.js",
    "sync:catalog": "node scripts/fetch-sanity.js && node scripts/import-leboncoin.js && node scripts/import-vinted.js && node scripts/sync-catalog.js",
    "build": "npm run sync:catalog",
    "dev": "node scripts/dev-server.js"
  },
  "dependencies": {
    "@sanity/client": "^6.21.0"
  }
}
